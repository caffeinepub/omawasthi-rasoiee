// RegistrationModal — kept as stub. User registration now happens on UserAuthPage.
interface RegistrationModalProps {
  open: boolean;
  onComplete: () => void;
}

export default function RegistrationModal({
  open: _open,
  onComplete: _onComplete,
}: RegistrationModalProps) {
  return null;
}
